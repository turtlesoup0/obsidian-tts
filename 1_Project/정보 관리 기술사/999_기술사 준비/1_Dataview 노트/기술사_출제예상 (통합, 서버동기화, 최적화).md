---
해시태그: "#검색제외"
---

- "기술사" 란 해당 기술 분야에 관한 고도의 **전문지식**과 *실무경험*에 입각한 **응용능력**을 보유한 사람으로서, **국가기술자격법 제10조**에 따라 *기술사 자격*을 *취득한 사람*을 말한다.

# 링크
## 참고자료
- [[0_기술사 활용 메타]]
- [[멘토링 노트]]

## 도메인 별 구성
- [[SW]]
- [[경영 관리]]
- [[데이터베이스]]
- [[빅데이터_인공지능 (AI, Artifical Intelligent)]]
- [[신기술]]
- [[네트워크]]
- [[데이터 분석]]
- [[정보보안 (Security)]]
- [[CA OS]]

## 용도별 구성
- [[표준 모음 (De Jure)]]
- [[규정, 가이드, 법 개정안 모음]]
- [[가트너 트렌드 용어]]

# Dataview

```dataviewjs
// ================================================================
// [캐시 재부착] Dataview 재렌더링 시 DOM 보존 (접근법 C)
// ================================================================
console.log('🏁 [DV-CACHE] 코드블록 실행 시작, 캐시 존재?', !!window._integratedNoteCache?.table);
if (window._integratedNoteCache?.table) {
    const cache = window._integratedNoteCache;

    // 메타데이터 해시 비교 (쿼리와 동일한 조건)
    let TAG_QUERY_CHECK = '#출제예상';
    const EXAM_START = 134, EXAM_END = 137;
    for (let i = EXAM_START; i <= EXAM_END; i++) {
        TAG_QUERY_CHECK += ` or #${i}관 or #${i}응`;
    }
    const freshPages = dv.pages(`"1_Project/정보 관리 기술사" and -#검색제외 and (${TAG_QUERY_CHECK})`)
        .sort(b => [b.file.folder, b.file.name], 'asc');

    const newHash = freshPages.map(p =>
        `${p.file.name}|${p.file.mtime}|${p.키워드 || ''}|${p.이미지 || ''}|${p.리드문 || ''}`
    ).join('\n');

    const hashMatch = (newHash === cache.hash);
    console.log('🏁 [DV-CACHE] 해시 비교:', hashMatch ? '동일' : '변경됨',
        `(pages: ${freshPages.length})`);

    if (hashMatch) {
        dv.container.appendChild(cache.table);
        if (cache.style && !cache.style.isConnected) {
            dv.container.appendChild(cache.style);
        }
        console.log('⚡ [DV-CACHE] 캐시 재부착 완료 - 재렌더링 스킵',
            { connected: cache.table.isConnected, rows: cache.table.querySelectorAll('tbody tr').length });
        return;
    }

    window._integratedNoteCache = null;
    console.log('🔄 [DV-CACHE] 메타데이터 변경 → 캐시 무효화, 전체 재렌더링');
}

// ================================================================
// 🧩 공유 모듈 로딩
// ================================================================
await dv.view("views/tts-core");
await dv.view("views/tts-config");  // 🔑 Config 로드 (필수)
await dv.view("views/tts-bell");
await dv.view("views/sse-sync");    // 🚀 SSE 실시간 동기화 (SPEC-PERF-001)

// ================================================================
// [1] 설정 (Configuration) - 동적 엔드포인트 사용
// ================================================================
const getAzureFunctionUrl = () => {
    // tts-config에서 설정된 URL 사용 (우선순위: config > localStorage > 기본값)
    if (window.ttsEndpointConfig?.azureFunctionUrl) {
        return window.ttsEndpointConfig.azureFunctionUrl;
    }
    if (window.ACTIVE_BASE_URL) {
        return window.ACTIVE_BASE_URL;
    }
    // 폴백: 기본 Azure Function URL
    return 'https://obsidian-tts-func-hwh0ffhneka3dtaa.koreacentral-01.azurewebsites.net';
};

const CONFIG = {
    EXAM_RANGE: { start: 134, end: 137 },
    AZURE_FUNCTION_URL: getAzureFunctionUrl(),  // 동적 설정 사용
    BREAKPOINTS: { mobile: 768, tablet: 1150 },
    SEARCH_DEBOUNCE_MS: 300,
    RESIZE_DEBOUNCE_MS: 500,
    STYLE_ID: 'integrated-note-style',
};
window.ttsLog('✅ [통합노트] CONFIG 로드 완료');
window.ttsLog('✅ [통합노트] Azure Function URL (동적):', CONFIG.AZURE_FUNCTION_URL);

// ================================================================
// 🧩 스크롤 매니저 모듈 로딩
// ================================================================
await dv.view("views/scroll-manager", { config: CONFIG });

// ================================================================
// 🚀 SSE 실시간 동기화 초기화 (SPEC-PERF-001)
// ================================================================
if (window.sseSyncManager) {
    const edgeServerUrl = window.ObsidianTTSConfig?.edgeServerUrl
        || window.ttsEndpointConfig?.edgeServerUrl
        || 'http://100.107.208.106:5051';

    window.sseSyncManager.init(edgeServerUrl).then(success => {
        if (success) {
            console.log('✅ [통합노트] SSE 모드 활성화 - 실시간 동기화');
        } else {
            console.log('⚠️ [통합노트] SSE 불가 - 폴링 모드 사용');
        }
    });
} else {
    window.ttsLog?.('⚠️ [통합노트] sseSyncManager 없음 - SSE 비활성화');
}

// TTS 재생 위치 읽기 엔드포인트 (tts-position 모듈과 동일한 동적 설정 사용)
const TTS_POSITION_READ_ENDPOINT = CONFIG.AZURE_FUNCTION_URL + '/api/playback-position';

// 검증 로그: TTS v5와 통합 노트가 동일한 엔드포인트 사용하는지 확인
if (window.playbackPositionManager?.apiEndpoint) {
    const ttsV5Endpoint = window.playbackPositionManager.apiEndpoint;
    const match = (ttsV5Endpoint === TTS_POSITION_READ_ENDPOINT);
    window.ttsLog('✅ TTS Position Read Endpoint (통합 노트):', TTS_POSITION_READ_ENDPOINT);
    window.ttsLog('✅ TTS v5 Endpoint:', ttsV5Endpoint);
    window.ttsLog(match ? '✅ 엔드포인트 일치 확인!' : '⚠️ 엔드포인트 불일치 감지!', {
        integrated: TTS_POSITION_READ_ENDPOINT,
        ttsV5: ttsV5Endpoint
    });
} else {
    window.ttsLog('✅ TTS Position Read Endpoint (통합 노트):', TTS_POSITION_READ_ENDPOINT);
    window.ttsLog('⚠️ TTS v5 모듈이 아직 로드되지 않음 (엔드포인트 비교 불가)');
}

// ================================================================
// [2] 데이터 쿼리
// ================================================================
let TAG_QUERY = '#출제예상';
for (let i = CONFIG.EXAM_RANGE.start; i <= CONFIG.EXAM_RANGE.end; i++) {
    TAG_QUERY += ` or #${i}관 or #${i}응`;
}

const pages = dv.pages(`"1_Project/정보 관리 기술사" and -#검색제외 and (${TAG_QUERY})`)
    .sort(b => [b.file.folder, b.file.name], 'asc');

const syncedPosition = window.scrollPositionManager
    ? await window.scrollPositionManager.syncPosition()
    : { noteName: localStorage.getItem('scroll_lastNoteName') || '', noteIndex: parseInt(localStorage.getItem('scroll_lastNoteIndex') || '-1', 10) };
const savedNoteName = syncedPosition.noteName || '';

// ================================================================
// [3] 검색/필터 엔진
// ================================================================
const domainSet = new Set();
const pageFolders = [];
pages.forEach(p => {
    const folder = p.file.folder;
    pageFolders.push(folder);
    const parts = folder.split('/');
    const domainIdx = parts.indexOf('정보 관리 기술사');
    if (domainIdx >= 0 && parts.length > domainIdx + 1) {
        domainSet.add(parts[domainIdx + 1]);
    }
});
const domains = [...domainSet].sort();

// ================================================================
// [4] 레이아웃 & 렌더링
// ================================================================
const getLayoutMode = () => {
    const width = window.innerWidth;
    if (width < CONFIG.BREAKPOINTS.mobile) return 'mobile';
    if (width < CONFIG.BREAKPOINTS.tablet) return 'tablet';
    return 'desktop';
};

const pageNames = [];
let bookmarkIndex = -1;
let currentLayoutMode = getLayoutMode();
window.ttsLog(`📊 총 ${pages.length}개 노트 렌더링 시작 (레이아웃: ${currentLayoutMode})`);

// 항상 3컬럼 데이터 생성 (CSS가 레이아웃 모드에 따라 표시/숨김)
const tableRows = pages.map((p, idx) => {
    pageNames.push(p.file.name);
    if (savedNoteName && p.file.name === savedNoteName) bookmarkIndex = idx;

    const prefix = (idx === bookmarkIndex) ? "📍 **[여기부터]**<br><br>" : "";

    // ===== 이미지 처리 (Dataview Link 객체 지원) =====
    // 모바일(1컬럼)에서는 즉시 로드, PC에서는 Lazy Loading
    const isMobile = window.innerWidth < CONFIG.BREAKPOINTS.mobile;
    let imageContent = '';
    if (p.이미지) {
        try {
            const imageLinks = Array.isArray(p.이미지) ? p.이미지 : [p.이미지];

            const imageHtmls = imageLinks.map(imgLink => {
                // Dataview Link 객체 확인 및 경로 추출
                const imagePath = imgLink?.path;
                if (!imagePath) return '';

                // app 객체 사용 가능 여부 확인
                if (typeof app === 'undefined' || !app?.vault) {
                    console.warn('[이미지] app 객체를 사용할 수 없습니다');
                    return '';
                }

                // TFile 객체 가져오기 (path는 상대 경로)
                const linkDest = app.metadataCache.getFirstLinkpathDest(imagePath, p.file.path);
                if (!linkDest) {
                    console.warn('[이미지] 파일을 찾을 수 없음:', imagePath);
                    return '';
                }

                // 리소스 경로 생성
                const imgPath = app.vault.getResourcePath(linkDest);

                // 모바일: 즉시 로드, PC: Lazy Loading
                if (isMobile) {
                    return `<img src="${imgPath}" decoding="async" style="max-width:100%;height:auto;display:block;margin-top:8px">`;
                } else {
                    return `<img class="lazy-image" data-src="${imgPath}" data-row="${idx}" src="data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7" decoding="async" style="max-width:100%;height:auto;display:block;min-height:150px;background:var(--background-modifier-border)">`;
                }
            }).filter(html => html);

            imageContent = imageHtmls.join('<br>');
        } catch (err) {
            console.error('[이미지] 처리 오류:', err);
            imageContent = '';
        }
    }

    const keywordText = Array.isArray(p.키워드) ? p.키워드.join('<br>') : (p.키워드 || '');

    const col1 = `${prefix}[[${p.file.name}]]<br><br>${p.리드문}<br><br>${p.정의}<br><br>${p.해시태그}`
        + (imageContent ? `<div class="in-inline-media">${imageContent}</div>` : '')
        + (keywordText ? `<div class="in-inline-keywords">---<br>${keywordText}</div>` : '');

    const col2 = keywordText
        + (imageContent ? `<div class="in-col2-media"><br>${imageContent}</div>` : '');

    const col3 = imageContent || '';

    return [col1, col2, col3];
});

window.currentPageNames = pageNames;
window._integratedNotePageFolders = pageFolders;

// 항상 3컬럼으로 렌더링
dv.table(["파일명", "키워드", "이미지"], tableRows);

// 저사양 디바이스 감지
const isLowEndDevice = () => {
    const canvas = document.createElement('canvas');
    const gl = canvas.getContext('webgl') || canvas.getContext('experimental-webgl');
    if (!gl) return true;
    const debugInfo = gl.getExtension('WEBGL_debug_renderer_info');
    if (debugInfo) {
        const renderer = gl.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL);
        if (/Adreno \(TM\) [3-4]\d\d|Mali-[4-5]\d\d/i.test(renderer)) return true;
    }
    if (navigator.hardwareConcurrency && navigator.hardwareConcurrency <= 4) return true;
    return false;
};
const lowEndMode = isLowEndDevice();
window.lowEndMode = lowEndMode;
window.ttsLog(`📱 디바이스 모드: ${lowEndMode ? '저사양 최적화' : '고성능'}`);

// ================================================================
// 🧩 UI 모듈 로딩
// ================================================================
await dv.view("views/integrated-ui", {
    config: CONFIG,
    domains,
    lowEndMode,
    getLayoutMode,
    TTS_POSITION_READ_ENDPOINT,
    bookmarkIndex,
    pages: pages.array(),
    savedNoteName,
    dv
});

// ================================================================
// [캐시 저장] dv.table() + integrated-ui 완성 후 DOM 캐시
// MutationObserver로 테이블 렌더링 완료를 감지하여 저장
// ================================================================
const saveCacheWhenReady = () => {
    const table = dv.container.querySelector('.table-view-table');
    if (!table || !table.querySelector('tbody tr')) {
        // 아직 렌더링 안 됨 → 500ms 후 재시도 (최대 10회)
        if (!window._cacheSaveRetry) window._cacheSaveRetry = 0;
        if (window._cacheSaveRetry < 10) {
            window._cacheSaveRetry++;
            setTimeout(saveCacheWhenReady, 500);
        }
        return;
    }
    window._cacheSaveRetry = 0;

    // 해시 생성 (상단 캐시 확인 로직과 동일한 기준)
    let TAG_QUERY_SAVE = '#출제예상';
    const EXAM_START = 134, EXAM_END = 137;
    for (let i = EXAM_START; i <= EXAM_END; i++) {
        TAG_QUERY_SAVE += ` or #${i}관 or #${i}응`;
    }
    const currentPages = dv.pages(`"1_Project/정보 관리 기술사" and -#검색제외 and (${TAG_QUERY_SAVE})`)
        .sort(b => [b.file.folder, b.file.name], 'asc');
    const hash = currentPages.map(p =>
        `${p.file.name}|${p.file.mtime}|${p.키워드 || ''}|${p.이미지 || ''}|${p.리드문 || ''}`
    ).join('\n');

    // style 요소 캐시 (container 내부 CSS)
    const style = dv.container.querySelector('style') || dv.container.querySelector(`#${CONFIG?.STYLE_ID}`);

    window._integratedNoteCache = { table, style, hash };
    console.log(`📌 [DV-CACHE] 캐시 저장 완료 (${table.querySelectorAll('tbody tr').length}행, hash길이: ${hash.length})`);
};
setTimeout(saveCacheWhenReady, 2000);
```

